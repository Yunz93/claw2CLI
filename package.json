{
  "name": "claw2cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local CLI bridge sidecar for OpenClaw on macOS",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  }
}
